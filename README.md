# 🖥 Telegram Server Monitor Bot

Telegram бот для мониторинга Linux сервера с автообновлением статуса каждые 5 минут.

## 🎯 Возможности

### 📊 Мониторинг
- **CPU** - использование процессора
- **RAM** - использование памяти
- **Disk** - использование диска
- **Network** - сетевой трафик
- **Uptime** - время работы
- **Load Average** - средняя нагрузка
- **Службы** - все работающие systemd службы
- **Порты** - все открытые порты с процессами

### 🎮 Команды управления
- `/status` - Текущий статус сервера
- `/services` - Список всех работающих служб
- `/ports` - Список открытых портов
- `/restart_service <имя>` - Перезапустить службу
- `/reboot` - Перезагрузить сервер
- `/logs <служба> [строк]` - Просмотр логов
- `/clear_logs` - Очистка логов journalctl
- `/close_port <номер>` - Закрыть порт (завершить процесс)

### ✨ Особенности
- **Автообновление** - сообщение в канале обновляется каждые 5 минут (не спамит!)
- **Защита** - только админы могут выполнять команды управления
- **Подтверждения** - все критичные действия требуют подтверждения
- **Эмодзи статусы** - 🟢 (норм) 🟡 (средне) 🔴 (критично)

## 📦 Установка

### Шаг 1: Скачай файлы на сервер

```bash
# Скопируй все файлы в одну папку на сервере
cd ~
mkdir telegram-bot
cd telegram-bot
# Загрузи сюда файлы или используй git clone если выложишь на GitHub
```

### Шаг 2: Создай бота в Telegram

1. Найди [@BotFather](https://t.me/BotFather) в Telegram
2. Отправь `/newbot`
3. Придумай имя и username для бота
4. **Скопируй токен** который даст BotFather (типа `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Шаг 3: Узнай свой Telegram ID

1. Найди [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправь `/start`
3. **Скопируй свой ID** (число типа `123456789`)

### Шаг 4: Запусти установку

```bash
sudo ./install.sh
```

Скрипт установит все зависимости и настроит автозапуск.

### Шаг 5: Настрой конфигурацию

Отредактируй файл бота:

```bash
sudo nano /opt/server-monitor-bot/server_monitor_bot.py
```

Найди в начале файла и измени:

```python
BOT_TOKEN = "ТУТ_ВСТАВЬ_ТОКЕН_ОТ_BOTFATHER"
ADMIN_IDS = [123456789]  # Твой ID от @userinfobot
UPDATE_INTERVAL = 300  # 5 минут, можешь изменить
```

**Сохрани:** `Ctrl+O`, `Enter`, `Ctrl+X`

### Шаг 6: Запусти бота

```bash
# Запускаем
sudo systemctl start server-monitor-bot

# Добавляем в автозагрузку
sudo systemctl enable server-monitor-bot

# Проверяем статус
sudo systemctl status server-monitor-bot
```

Если все ОК, увидишь `active (running)` и `🤖 Бот запущен!`

### Шаг 7: Добавь бота в канал

1. Создай канал в Telegram (или используй существующий)
2. Добавь бота в канал как **администратора**
3. Дай ему права:
   - ✅ Публикация сообщений
   - ✅ Редактирование сообщений
   - ✅ Удаление сообщений

**Бот сразу отправит первое сообщение со статусом!**

## 🎮 Использование

### Мониторинг в канале

После добавления бота в канал, он:
1. Отправит сообщение со статусом сервера
2. Каждые 5 минут будет **редактировать** это же сообщение
3. Никакого спама - одно сообщение постоянно обновляется!

### Команды в личке боту

Напиши боту в личку любую команду:

```
/status - Моментальный статус
/services - Список служб
/ports - Открытые порты
/restart_service nginx - Перезапустить nginx
/logs nginx 100 - Последние 100 строк логов nginx
/close_port 8080 - Закрыть порт 8080
/reboot - Перезагрузить сервер (с подтверждением!)
```

## 🔧 Управление

### Просмотр логов бота

```bash
# Реал-тайм логи
sudo journalctl -u server-monitor-bot -f

# Последние 100 строк
sudo journalctl -u server-monitor-bot -n 100
```

### Перезапуск бота

```bash
sudo systemctl restart server-monitor-bot
```

### Остановка бота

```bash
sudo systemctl stop server-monitor-bot
```

### Обновление бота

1. Отредактируй код:
```bash
sudo nano /opt/server-monitor-bot/server_monitor_bot.py
```

2. Перезапусти:
```bash
sudo systemctl restart server-monitor-bot
```

## ⚙️ Настройка

### Изменить интервал обновления

В файле `/opt/server-monitor-bot/server_monitor_bot.py`:

```python
UPDATE_INTERVAL = 300  # Секунды (300 = 5 минут)
```

Варианты:
- `60` - каждую минуту
- `300` - каждые 5 минут (по умолчанию)
- `600` - каждые 10 минут

### Добавить админов

```python
ADMIN_IDS = [123456789, 987654321, 555666777]  # Список ID админов
```

### Мониторить конкретные службы

Можешь добавить фильтрацию в функцию `get_running_services()` в коде.

## 📋 Примеры использования

### 1. Перезапуск упавшей службы

```
Ты: /restart_service nginx
Бот: ⚠️ Вы уверены что хотите перезапустить службу nginx?
     [✅ Да] [❌ Отмена]
Ты: *нажимаешь Да*
Бот: ✅ Служба nginx успешно перезапущена
```

### 2. Проверка логов

```
Ты: /logs nginx 50
Бот: 📋 Логи nginx:
     [последние 50 строк логов]
```

### 3. Закрытие подозрительного порта

```
Ты: /close_port 4444
Бот: ⚠️ Вы уверены что хотите закрыть порт 4444?
     [✅ Да] [❌ Отмена]
Ты: *нажимаешь Да*
Бот: ✅ Порт 4444 закрыт (завершено процессов: 1)
```

## 🛡️ Безопасность

1. **ADMIN_IDS** - только указанные ID могут выполнять команды управления
2. **Подтверждения** - все критичные действия требуют подтверждения через кнопки
3. **Логи** - все действия записываются в systemd журнал
4. **Права** - бот работает от root (нужны для управления службами)

## 🐛 Решение проблем

### Бот не запускается

```bash
# Проверь логи
sudo journalctl -u server-monitor-bot -n 50

# Проверь что токен правильный
sudo nano /opt/server-monitor-bot/server_monitor_bot.py
```

### Бот не отвечает на команды

1. Проверь что бот запущен: `systemctl status server-monitor-bot`
2. Проверь что твой ID в `ADMIN_IDS`
3. Напиши `/start` боту

### Сообщение не обновляется в канале

1. Проверь что бот админ в канале
2. Проверь права бота (редактирование сообщений)
3. Проверь логи: `journalctl -u server-monitor-bot -f`

### Команды управления не работают

1. Убедись что ты админ (твой ID в `ADMIN_IDS`)
2. Проверь что у бота есть права sudo (работает от root)
3. Для некоторых команд нужен пакет `lsof`: `sudo apt install lsof`

## 📝 TODO / Идеи для улучшения

- [ ] Уведомления при критичных событиях (CPU > 90%, диск заполнен)
- [ ] График использования ресурсов
- [ ] Поддержка нескольких серверов
- [ ] Web интерфейс
- [ ] Экспорт метрик в Prometheus
- [ ] Алерты в Telegram при падении служб
- [ ] Backup и restore через бота
- [ ] Управление Docker контейнерами

## 📜 Лицензия

MIT License - делай что хочешь с кодом!

## 🤝 Помощь

Если что-то не работает - пиши, разберемся бро! 💪
